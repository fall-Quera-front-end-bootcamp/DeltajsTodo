import Calendar from '../components/Calendar copy/Calendar'

const Workspace = (): JSX.Element => {
  return (
    <div className="relative h-screen w-full bg-gray-primary">
      <Calendar />
    </div>
  )
}

export default Workspace
